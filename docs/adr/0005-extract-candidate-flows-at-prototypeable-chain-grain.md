# Extract candidate flows at prototypeable chain grain

Flowspec will extract candidate flows at the granularity of an independently prototypeable user chain, not at the granularity of individual screens or entire product areas. Each candidate should declare its entry, success exit, default path scope, included guardrails, and excluded paths so a designer can pick the core happy path first and request additional paths later.
