# Model prototype surface in source JSON

Flowspec will model prototype boundaries in `ux-flow-spec.json` instead of only rendering them into Markdown. This keeps implementation guidance, skeleton selection, and handoff constraints validateable and testable while preserving `prototype-handoff.md` as a rendered view of the source-of-truth JSON.
