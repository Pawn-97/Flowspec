# Require downstream implementation evidence

Flowspec v0.1 handoff will require downstream prototype agents to report implementation evidence for each FlowSpec component: the component id, concrete UI implementation mapping, and any material deviation from the requested `implementation_target`. `prototype-handoff.md` will also include an acceptance checklist so FlowSpec coverage can be reviewed without turning Flowspec itself into a prototype QA tool.
